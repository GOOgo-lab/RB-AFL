# Results

No manuscript result is fabricated or bundled in this code-only release. Formal outputs are generated below the configured `runs/` directory and should be deposited separately with hashes after the 111-identity, ten-seed experiment completes.

