# Required data

Place exactly 111 independent source identities in `data/raw/` and the actual original 45-one/211-zero watermark in `data/watermark.png`. No donor is required by the four default attack families. Keep original data provenance, source hashes, and the identity split. These local inputs are ignored by Git; see `docs/DATA.md`.
